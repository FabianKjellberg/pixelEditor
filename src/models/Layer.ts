export interface Layer {
  xPos: number;
  yPos: number;
  width: number;
  height: number;

  pixels: Uint32Array;
}

export interface Cordinate {
  x: number;
  y: number;
}
export interface GrowthNumbers {}
